name: workflow to deploy a lamp stack

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-resources-to-azure:
    runs-on: ubuntu-latest
    environment:
     name: staging.azure
    steps:
        - name: Checkout master
          uses: actions/checkout@v3
          
        - name: az cli login
          uses: azure/login@v1
          with:
            client-id: ${{ secrets.AZURE_CLIENT_ID }}
            tenant-id: ${{ secrets.AZURE_TENANT_ID }}
            subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
            enable-AzPSSession: false

         # Deploy Bicep file
        - name: deploy bicep file
          uses: azure/arm-deploy@v1
          with:
            subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
            resourceGroupName: ${{ secrets.AZURE_RG }}
            template: https://raw.githubusercontent.com/kaushikenterpriseorg/workflowsprotorepo/main/infra/wordpress-single-vm-ubuntu/azuredeploy.json?token=GHSAT0AAAAAACHH2X77IG7ZXCEZJDMTBQZ2ZHY73R
            parameters: https://raw.githubusercontent.com/kaushikenterpriseorg/workflowsprotorepo/main/infra/wordpress-single-vm-ubuntu/azuredeploy.parameters.json?token=GHSAT0AAAAAACHH2X77IG7ZXCEZJDMTBQZ2ZHY73RA
            failOnStdErr: false
      
        
